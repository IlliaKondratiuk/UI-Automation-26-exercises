//This class is used with the @Category annotation in test classes for exercises 1 to 12.
//This enables separate test execution using "mvn test -Dgroups=CriticalTests"
//Yet, all tests from 1 to 12 are critical, so all of them have this category
package config;

public interface CriticalTests {
}
